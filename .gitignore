node_modules/
.next/
out/
build/

.DS_Store
*.log
.env*.local

# Lift pipeline intermediates. .lift/src holds the template exports the
# pipeline reads and is committed, so the generated code is reproducible.
.lift/*
!.lift/src/
__pycache__/
*.pyc

# Original project screenshots (~59MB). public/projects holds the
# web-sized WebP the site actually ships; re-run tools/optimize_images.py
# against these to regenerate them.
projects/
