node_modules/
.next/
out/
build/

.DS_Store
*.log
.env*.local

# Lift pipeline intermediates. .lift/src holds the template exports the
# pipeline reads and is committed, so the generated code is reproducible.
.lift/*
!.lift/src/
__pycache__/
*.pyc

# Original screenshots and the untrimmed portrait (~62MB). Anchored to the
# repo root so it does not also match public/projects, which is committed.
/projects/

# Static export output
out/
